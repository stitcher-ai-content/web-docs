---
title: "Connections & credentials"
description: "How to define a data connection in StitcherAI, why credentials live in the web app and never in your configuration repository, how connections are validated before use, and where to find ready-made per-provider setup."
---

A connection tells StitcherAI where your data lives and how to reach it. You define connections in the web app, where credentials are stored securely and validated before anything depends on them.

## Credentials live in the web app

<Warning>
Data-source credentials are entered and managed in the web app — **never** in your configuration repository. Your cost model refers to a connection only by its **name**; the actual keys and roles are held in StitcherAI. Nothing sensitive is ever committed alongside the model.
</Warning>

This separation lets you version-control and peer-review the cost model itself (see [Authoring & version control](/authoring/version-control)) without ever exposing a secret in a pull request. When a run executes, StitcherAI resolves the connection name to its stored credentials at that moment.

## Define and validate a connection

<Steps>
  <Step title="Provision provider access">
    Create the least-privilege access the provider needs — a role, service account, or API key. Use the ready-made infrastructure below so you don't have to assemble permissions by hand.
  </Step>
  <Step title="Create the connection in the web app">
    Enter the location and credentials. They're stored in StitcherAI, referenced from your model by name only.
  </Step>
  <Step title="Validate before use">
    StitcherAI checks the connection against the provider to confirm it can actually read — or, for a destination, write — the data.
  </Step>
  <Step title="Craft sources from it">
    A connection created with no dataset attached is a **shared, multi-purpose source connection**. From it you craft as many [data sources](/data-foundation/overview#a-shared-multi-purpose-source-connection) as you need in the StitcherAI Editor — each declared cost or business at the point of use — without re-entering connection details. For a shared connection, you supply a representative path or table so validation has something to check.
  </Step>
</Steps>

<Info>
Validation runs **when you create the connection**, not when a run later fails. On success you're cleared to use it; on failure StitcherAI reports a clear, actionable reason — for example, a missing storage permission or an expired key — so you can fix the access and revalidate.
</Info>

## Per-provider setup, ready to use

The full list of providers — what each ingests, how it's retrieved, and what's coming next — is on [Supported integrations](/data-foundation/integrations). Each provider has its own access requirements. StitcherAI publishes detailed, copy-pasteable infrastructure — least-privilege roles, service accounts, and storage grants — in a public Terraform-modules repository, so you provision exactly what each connector needs and nothing more.

<Card title="StitcherAI Terraform modules" icon="github" href="https://github.com/StitcherAI/tf-modules">
  Per-provider setup modules — provision the access, then reference the connection in StitcherAI.
</Card>

What each family of providers needs, at a glance:

<Tabs>
  <Tab title="Cloud storage & warehouse">
    - **AWS** — a cross-account role with read access to the cost-export bucket, plus an external id.
    - **Azure** — an app identity with storage roles scoped to the export path.
    - **Google Cloud** — a service account with read access to BigQuery and / or the storage bucket.
    - **Snowflake** — a user, role, and warehouse with read access to the usage views, granted with least-privilege SQL.
    - **Databricks** — a service principal with read access to the usage report.
  </Tab>
  <Tab title="Provider APIs">
    - **OpenAI · Anthropic / Claude · Twilio · Confluent Cloud · Elastic Cloud · MongoDB Atlas** — an API key created in the provider's console with permission to read usage and cost (an Admin or Analytics key for the AI vendors), entered directly in the web app.

    Where a provider needs a small extra input — a discount %, an ECU rate, a prepaid balance — you supply it on the source in the Connect phase; see [Supported integrations](/data-foundation/integrations).
  </Tab>
  <Tab title="File & PDF sources">
    - **Any vendor invoice or custom cost file** — read access to the storage location (S3, GCS, or Azure Storage) where the files land. One shared connection to that location can back both cost files and business-data files.

    For how unstructured files become FOCUS, see [AI-powered ingestion](/data-foundation/ai-ingestion).
  </Tab>
</Tabs>

<Tip>
Pushing data from your own side? Customer-side sync tools write to **your** storage with **your** keys, and a StitcherAI file connection then reads from that location — your bucket credentials are never given to StitcherAI. The connection you define here is the read side of that handoff.
</Tip>

## Next

<CardGroup cols={3}>
  <Card title="Datasets, connectors & data connections" icon="plug" href="/data-foundation/overview">
    The building blocks a connection plugs into.
  </Card>
  <Card title="Supported integrations" icon="list-check" href="/data-foundation/integrations">
    The full provider catalog and per-family access setup.
  </Card>
  <Card title="FOCUS" icon="layer-group" href="/data-foundation/focus">
    The open cost language your connected sources are normalized into.
  </Card>
</CardGroup>
